# 任务提取规则

数据源：`atlas-ai-docs/tasks.md`

## 必需行为

- 从 `atlas-ai-docs/tasks.md` 读取任务索引。
- 每个链接任务条目对应一行任务。
- 保留索引中的 `Summary` 作为任务主简述。
- `Summary` 直接基于当前 DOC 内容翻译成中文，不使用硬编码映射。
- 将任务拆分成未完成和已完成两组展示。
- 任务名使用任务目录名本身，不使用 alias。
- 组内按任务目录名前缀日期从新到旧排序。
