interface:
  display_name: "Create Project Task"
  short_description: "Create a project-manager task directory"
  default_prompt: "Use $create-task to create a new project task directory from project-template and update the workspace indexes."
